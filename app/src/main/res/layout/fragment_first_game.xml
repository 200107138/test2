<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    >
<data>
    <import type="android.graphics.drawable.Drawable" />
    <variable
        name="gameViewModel"
        type="com.example.test.FirstGameViewModel" />
    <variable
        name="maxNoOfWords"
        type="int" />
</data>
    <androidx.constraintlayout.widget.ConstraintLayout
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="16dp"
        tools:context="com.example.test.FirstGameFragment"
        android:background="#FDFDFD">
    <TextView
        android:id="@+id/firstgamecount"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@{@string/game_round_count(gameViewModel.currentFirstGameCount, maxNoOfWords)}"
        android:textAlignment="center"
        android:textSize="25dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="Round 0 of 3"/>

    <TextView
        android:id="@+id/first_game_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:padding="20dp"
        android:text="@string/first_game_description"
        android:textAlignment="center"
        android:textSize="25dp"

        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/firstgamecount" />

    <TextView
        android:id="@+id/firstgamestarttext"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:background="@{gameViewModel.background ? @drawable/first_game_start_green : @drawable/first_game_start}"
        android:gravity="center"
        tools:background="@drawable/first_game_start"
        android:padding="16dp"
        android:tag="white"
        android:text="@{gameViewModel.starttext}"
        android:textSize="100dp"

        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/first_game_text" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>